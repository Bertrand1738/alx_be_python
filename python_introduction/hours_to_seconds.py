hours = 2
# The code defines a variable named hours and assigns it the value 2.
seconds = hours * 3600
# The code converts the given hours to seconds.

print (f"{hours} hour(s) is {seconds} seconds.")
# The code prints the result using an f-string, which allows you to embed expressions inside a string.  
